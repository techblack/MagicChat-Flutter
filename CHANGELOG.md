# 更新日志

## 0.2.9

- 修复实时引用回复首帧显示占位或原始消息 ID，补齐原消息后即时刷新为正文和成员名称；复杂引用消息显示对应类型摘要。
- 宽屏聊天顶栏继续横跨完整窗口宽度，聊天与话题相关展示统一隐藏内部消息、会话和用户 ID 回退。
- Android APK/AAB 发布继续使用固定 Release 证书，并在流水线中校验证书指纹；客户端版本更新为 `0.2.9`。

## 0.2.8

- Windows、macOS、Linux 默认从 GitHub Release 检查并选择对应平台安装包。
- 自定义更新源继续支持编译时 HTTPS manifest 配置。
- 更新检查与推送注册客户端版本兜底同步为 `0.2.8`。

## 0.2.7

- 格式刷支持按 `Escape` 快速取消，避免误操作后必须重新点击工具栏。
- 更新检查与推送注册客户端版本兜底同步为 `0.2.7`。

## 0.2.6

- 格式刷同时复制文字格式和段落对齐方式，行为与官方桌面端保持一致。
- Android、iOS 推送注册的客户端版本兜底同步为 `0.2.6`。

## 0.2.5

- 未选中会话时显示明确的聊天引导状态。
- 富文档支持格式刷，可将一个文本块的完整格式应用到另一个文本块。
- 消息长按菜单支持复制单条消息，并自动将提及标记转换为联系人名称。
- 推送注册的客户端版本兜底与当前应用版本保持一致。

## 0.2.4

- 宽屏聊天顶栏独立覆盖完整窗口宽度，移动端继续覆盖顶部安全区。
- 引用消息统一解析嵌套正文、发送者和序号；缺少摘要时自动回溯历史消息，避免显示原始消息 ID。
- 好友申请、消息搜索、话题和消息详情继续以联系人名称和可读内容替代内部 ID。
- 富文档支持图片占位插入、临时文件上传、HTTPS 外链、替代文本、宽度和对齐设置。
- Android APK/AAB 发布继续使用固定 Release 证书，并在流水线中校验证书指纹。

## 0.2.3

- 文本、图片、文件和语音发送后立即显示，失败时可一键重试，并复用客户端消息 ID 与服务端结果对账。
- 宽屏聊天顶栏横跨完整消息区域，移动端继续完整覆盖顶部安全区。
- 引用回复可按消息序号预取分页外原文，优先展示对应消息内容和成员名称。
- 长文本与 Markdown 消息支持折叠展开，复杂消息进入聊天页前完成预加载。
- 群聊顶栏展示成员总数和群公告，私聊与应用会话支持实时“正在输入”状态。
- 搜索结果和应用访问范围等界面进一步以名称替代内部 ID。
- Android 通用/分 ABI APK 与 AAB 继续使用固定 Release 证书，发布流水线会逐个校验产物证书指纹。

该版本由 GitHub Actions 根据 `v0.2.3` 标签构建。Android 使用固定 Release 证书签名，iOS 产物未签名，macOS 产物未经 Apple 公证，具体限制见 README。

## 0.2.2

- 聊天顶栏在紧凑与宽屏布局中占满整个会话区域，顶部背景完整覆盖安全区。
- 引用回复优先显示对应消息正文和联系人名称，并继续收敛提及、联系人与账户展示中的原始 ID。
- 图片消息支持同会话全屏画廊、前后切换、相邻预取、跨页查找、缩放、保存、转发和失败重试。
- 新增独立聊天详情，完善群成员、群公告、群头像、加群与私聊转群等管理能力。
- 完善服务器管理、邮箱验证码账号注销及跨平台存储空间管理。
- Android 通用/分 ABI APK 与 AAB 继续使用固定 Release 证书，发布流水线会逐个校验产物证书指纹。

该版本由 GitHub Actions 根据 `v0.2.2` 标签构建。Android 使用固定 Release 证书签名，iOS 产物未签名，macOS 产物未经 Apple 公证，具体限制见 README。

## 0.2.1

- 聊天页顶部栏横向占满窗口，并将背景延伸到移动端顶部安全区。
- 引用回复优先展示对应消息正文与联系人名称，兼容仅返回引用消息 ID 的响应。
- 会话摘要、搜索结果、话题摘要、资料卡、表情参与者及项目成员不再回退显示内部 ID。
- 消息详情、复制内容和转发摘要统一将提及标记转换为联系人名称。
- Android 通用/分 ABI APK 与 AAB 改用固定 Release 证书，并在发布前校验证书指纹。

该版本由 GitHub Actions 根据 `v0.2.1` 标签构建。Android 使用固定 Release 证书签名，iOS 产物未签名，macOS 产物未经 Apple 公证，具体限制见 README。

## 0.2.0

- 修复 Android 聊天页标题栏与系统状态栏重合及输入区多余空隙。
- 会话标题居中显示真实名称，并在移动端和桌面端提供会话内高级检索。
- 支持双击消息独立查看和选择复制、左滑引用回复，以及群聊成员资料卡。
- 支持 Enter 或 Ctrl/Command+Enter 发送快捷键设置。
- 图片消息按原始比例自适应气泡，不再重复显示 `[图片]` 占位。
- 消息气泡按内容长度调整宽度，加载更早消息时显示连贯进度提示。
- Native 使用按会话类型分库的 SQLite WAL 消息缓存，Web 保留持久化回退。
- 会话优先显示本地消息；实时消息先尝试写入缓存，缓存失败仍正常展示。
- 自己发送的实时消息不再计入未读、新消息提示或本地系统通知。
- 完善通知点击会话路由、应用内未读红点及 Android/iOS/macOS 系统角标。

该版本由 GitHub Actions 根据 `v0.2.0` 标签构建。Android 使用测试签名，iOS 产物未签名，macOS 产物未经 Apple 公证，具体限制见 README。

## 0.1.1

- 完善消息时间展示，并按最后消息时间排序会话。
- 支持编译时配置更新源，默认使用 release 更新源。
- 预加载图片和附件消息，减少聊天页面首屏布局跳动。
- 支持会话置顶和消息免打扰，并兼容实时状态更新。
- 改善 Web 第三方登录、实时会话列表、消息提醒和账户资料恢复体验。

该版本由 GitHub Actions 根据 `v0.1.1` 标签构建。Android 使用测试签名，iOS 产物未签名，macOS 产物未经 Apple 公证，具体限制见 README。
